# Network Resilience Flow Diagram

## 🎬 Complete Transaction Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER CLICKS "Send Money"                     │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              STEP 1: OPTIMISTIC UPDATE (0ms)                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. Cancel conflicting queries                              │  │
│  │ 2. Save current state snapshot                            │  │
│  │ 3. Add transaction to UI immediately                      │  │
│  │ 4. Show "Pending..." status                               │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ✅ User sees instant feedback (feels instant!)                │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              STEP 2: NETWORK REQUEST SENT                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ POST /api/transactions                                    │  │
│  │ { amount: 100, recipient: "Alice" }                      │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ⏱️  Chaos Mode: Random 0-5s delay                            │
└────────────────────────────┬────────────────────────────────────┘
                             │
                    ┌────────┴────────┐
                    │                 │
                    ▼                 ▼
        ┌──────────────────┐  ┌──────────────────┐
        │   SUCCESS (90%)  │  │    ERROR (10%)    │
        └────────┬─────────┘  └────────┬─────────┘
                 │                     │
                 ▼                     ▼
┌────────────────────────────┐  ┌────────────────────────────┐
│ STEP 3A: SUCCESS PATH      │  │ STEP 3B: ERROR PATH        │
│                            │  │                            │
│ 1. Replace temp ID with    │  │ 1. ROLLBACK: Remove        │
│    real server ID          │  │    optimistic transaction   │
│ 2. Update status to        │  │ 2. Restore previous state  │
│    "completed"             │  │ 3. Show error toast        │
│ 3. Show success toast      │  │ 4. Show "Retry" button    │
│                            │  │                            │
│ ✅ Transaction confirmed   │  │ ❌ Transaction failed      │
└────────────────────────────┘  └────────────┬───────────────┘
                                               │
                                               │ User clicks
                                               │ "Retry"
                                               ▼
                                    ┌──────────────────────┐
                                    │ RETRY: Go to Step 1  │
                                    │ (Same transaction)   │
                                    └──────────────────────┘
```

## 🔄 Network Problem Solutions

### Problem 1: Slow Network (High Latency)
```
Normal Flow:
User Click → [Wait 3 seconds] → UI Updates ❌ (Feels slow)

With Optimistic Updates:
User Click → UI Updates (0ms) → [Wait 3 seconds] → Confirms ✅ (Feels instant!)
```

### Problem 2: Network Failure
```
Without Rollback:
User Click → UI Shows Transaction → Network Fails → ❌ Wrong UI State

With Rollback:
User Click → UI Shows Transaction → Network Fails → Auto-Remove → ✅ Correct State
```

### Problem 3: User Needs to Retry
```
Without Retry:
Network Fails → User must re-enter all data → Click again ❌ (Frustrating)

With Retry:
Network Fails → Click "Retry" button → ✅ (One click, no re-entry)
```

## 🎯 Key Mechanisms

### 1. Optimistic UI Pattern
```
┌─────────────┐
│ User Action │
└──────┬──────┘
       │
       ▼
┌─────────────────┐      ┌──────────────┐
│ Update UI First │ ────▶ │ Network Call │
│ (Optimistic)    │       │ (Background) │
└─────────────────┘      └──────┬───────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
                    ▼                         ▼
            ┌──────────────┐          ┌──────────────┐
            │   Success    │          │    Error     │
            │ Confirm UI   │          │  Rollback UI │
            └──────────────┘          └──────────────┘
```

### 2. State Snapshot Pattern
```
Before Mutation:
┌─────────────────────┐
│ State Snapshot      │ ← Saved for rollback
│ [tx1, tx2, tx3]     │
└─────────────────────┘

After Optimistic Update:
┌─────────────────────┐
│ New State           │
│ [temp-tx, tx1, ...] │ ← Optimistic transaction
└─────────────────────┘

On Error:
┌─────────────────────┐
│ Restored State      │ ← Rolled back from snapshot
│ [tx1, tx2, tx3]     │
└─────────────────────┘
```

### 3. Retry Pattern
```
Failed Request:
┌─────────────────────┐
│ Error Toast         │
│ "Transaction Failed"│
│ [Retry Button]      │ ← Stores failed request
└─────────────────────┘

User Clicks Retry:
┌─────────────────────┐
│ Retry Same Request  │ ← No re-entry needed
│ (Same data)         │
└─────────────────────┘
```

## 📊 Comparison: With vs Without Resilience

| Scenario | Without Resilience | With Resilience |
|----------|-------------------|-----------------|
| **Slow Network** | User waits 3s, sees nothing | User sees instant feedback |
| **Network Error** | UI shows wrong state | UI auto-corrects |
| **User Retry** | Must re-enter everything | One-click retry |
| **Race Conditions** | Conflicting updates | Queries cancelled |
| **State Consistency** | Can be out of sync | Always matches server |

## 🧪 Testing with Chaos Mode

```
Chaos Mode OFF:
Request → [Instant] → Response ✅

Chaos Mode ON:
Request → [0-5s random delay] → Response
         ↓
    10% chance of 500 error
         ↓
    Tests rollback + retry ✅
```

This architecture ensures the app works reliably even when the network doesn't! 🚀